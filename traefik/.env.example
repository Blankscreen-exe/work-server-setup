# Traefik Dashboard BasicAuth Hash
# Generate with: htpasswd -nbB admin YOUR_PASSWORD
# Note: Store the hash with $$ for each $ sign (escaped for docker-compose)
# Example: If htpasswd outputs: `$2y$05$abc123...`
# Store here as: `$$2y$$05$$abc123...`
TRAEFIK_AUTH_HASH=$$2y$$05$$CHANGE_THIS_HASH
